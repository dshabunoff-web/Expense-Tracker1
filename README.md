Инициализация репозитория и первый коммит:
git init
git add expense_tracker.py
git commit -m "Initial: Expense Tracker with filters and JSON export/import"
Игнорирование временных файлов:
Создайте файл .gitignore и добавьте: *.pyc, pycache/, venv/
Регулярные коммиты:
git add .
git commit -m "Добавлена фильтрация, экспорт/импорт JSON и подсчёт суммы"
Расширения (по желанию)
хранение настроек в SQLite или отдельном JSON файле
графическое отображение сумм по категориям за выбранный период (matplotlib)
автоматическое резервное копирование истории в GitHub Gist или облако
поддержка локализации и форматирования чисел под локаль пользователя
